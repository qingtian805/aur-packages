name: Test QQ Version

on:
  workflow_dispatch:

jobs:
  fetch-qq-version:
    name: Print QQ version JS
    runs-on: ubuntu-latest
    steps:
      - name: Fetch and print linuxConfig.js
        run: |
          echo "--- Raw response from linuxConfig.js ---"
          curl -fsSL "https://cdn-go.cn/qq-web/im.qq.com_new/latest/rainbow/linuxConfig.js"
          echo ""
          echo "--- End of response ---"
